# Server configuration
PORT=8080
ENV=production

# Database configuration 
DB_USER=postgres
DB_PASSWORD=
DB_NAME=pixshelf

# Storage configuration
IMAGE_STORAGE=/app/static/images

# Base URL for image URLs - update this when deploying
BASE_URL=https://pixshelf.yourdomain.com
